# Building Vision model publication

## R04 — FF03 four-person sliding entrance

The 12 September request changes only the FF03 four-person cabin's west entrance to sliding. Preserve its original opening and fixed partitions, all six FF03 tables/chairs, cupboard, two-person cabin door, FF05 inward shared access and every other room. The separate unconfirmed exterior opening shift is not included. The new panels park beside the opening and project 40 mm beyond the fixed-frame plane; the overhead track projects 52.5 mm beyond that fixed-frame face, above the door head. Removing the outward swing is not a whole-corridor nonreduction, occupied-use or hardware-installation approval. The roughly 1 ft occupied rear-chair space remains constrained. S01 numerical service assumptions are unchanged, not recalculated.

R04 is an incremental publication. Its first-floor GLB, native Blender/first-floor CAD, FF03 extract and native previews are new. The ground-floor GLB/preview/CAD, FF04/FF06 native previews and the other fourteen room extracts retain their exact published R03 URLs, bytes and source attribution. R03 and R01 directories and historical concept images are immutable. The retained R03 files include their original R02 or R03 room provenance; do not relabel them as independently regenerated R04 designs.

`buildingModelRelease.json` identifies the floor assets and room-native previews explicitly. The R04 `release.json` separates new `files`, exact `retainedAssets` and verified `externalDownloads`, with a pinned previous manifest. `rooms/manifest.json` changes only FF03 and keeps the other fourteen entries identical to R03. The new exporter regenerates only the first-floor GLB; independent native re-import also compares the unchanged R03 ground GLB against the new cumulative Blender source. Use fresh saved-source/CAD matching, complete modeled motion, original-aperture and metadata checks; do not transfer the earlier P03 H1 corridor-recovery claim to FF03.

The FF03 parked and closed images are native Blender renders. Its top image is a horizontal display section at 1.05 m above the floor, not an unsectioned or surveyed plan. Native CAD renders and SVG wireframes remain separately labelled. New release acceptance pins are recorded only after reviewing the actual evidence. The normal build still requires downloaded-byte proof for new external native assets, and the live check covers new plus retained URLs. Preparation alone does not mean R04 is live.

The R04 public native files are byte-identical copies of already-current, freshly reopened P02 files, with serialized private-path scans. Their transformation is `verified-current-design-copy`, not a claim that metadata rewriting or historical curation occurred during copying. The FF03 room file retains separately attributed R03 reference members; those inherited Z0 references are not claimed to be new extractions from the curated full-floor file.

The public page now shows current model views only, with a data-derived gallery count. Historical concept files remain preserved but their dropdowns are removed. The four-decisions section, fixed 21-view stats, large service warning, and agent-instruction/prompt section are removed as requested. The footer uses the full HSR 1490 address including Karnataka.

## Previous R03 release and design history

**R03 candidate — P03 sliding entrances authorized.** On 11 September 2026 the user explicitly approved sliding doors and publication of the designs and webpage. This supersedes the earlier outward-entrance restriction and hold pending a door-detail revision. It does not authorize moving openings, cupboard/dresser, furniture, stairs or guards, or publishing failed private trials. Final native, geometry, curation, download and website gates are required before release; local preparation is not live publication.

P03 replaces H1/upper FF06 with two independent inside-parking leaves, H2/lower FF06 with one inside-parking leaf, and H3/right FF04 with a left-parking leaf. Retain the existing left FF04 slider and two curved stair leaves, with corrected return details. Preserve all sixteen tables and sixteen chair proxies. The P02 swing trial passed modeled motion but narrowed the H1 closed corridor strip from about 971 mm to 894 mm; it remains private failed-access history. P03 must independently recover the original local strip and pass complete stop-limited movement, bearing-support and parked-opening checks. Hardware remains a planning proposal, not an engineered installation or egress approval.

The frontend's active release and native URLs come from `src/data/buildingModelRelease.json`; the checker must validate the same release. Public native copies contain only current selected design, with private paths and failed history removed. Curation requires separate source/public hashes, exact retained geometry/materials, selected membership, display-only changes, dependency disclosure and independent native reopen. These are curated copies, not byte-identical private historical sources. Files over 25 MiB stay outside Pages and use the approved GitHub release, with actual remote byte/hash verification. `check:live-release` additionally compares the published manifest and all model/download bytes. R01 remains immutable public history; R02 was an unpublished checkpoint.

The Building Vision route preserves the 21 earlier concept PNGs and adds versioned engineering references. Ground floor and first floor are the only verified modeled levels. GF05 patio is excluded; no second-floor model may be inferred. Local preparation is not evidence of a live deployment.

## Image authority and alignment

### R03 verification completed before website release

- Selected P03 Blender source: `4d4964c3c42c3501ada1ae3274f18a686a190cd8207e8ea9b6df7245654bc2b6`. Its current-design-only public copy is `2bf4638a5d4ec2eaca76316debf06f290d69afe36f667fc8329247460f130a70`. Exactly two selected floor scenes remain; authored geometry, evaluated positions/topology, materials and saved review views pass fresh reopen. Evaluated modifier UV rounding is separately bounded and disclosed.
- New private cumulative CAD preserves 4,797 objects, including every prior baseline object. The curated first-floor copy contains 1,769 selected fit-out/context shapes and passes independent reopen. The public ground-floor copy contains 1,612 visible current source shapes. Public copies retain editable B-reps, not the entire historical parametric feature graph.
- Complete conservative slider-travel and intended-support-contact checks pass with zero collision or unresolved pairs. Local parked openings are not reduced; the H1 corridor strip is restored to approximately 971 mm on the same model basis. These are local modeled checks, not occupied-person, hardware-load, site or compliance certification. Public evidence: `public/building-models/r03/design-verification.json`.
- Both GLBs pass independent native re-import comparison. Six public native previews are bound to source hashes. The FF04 focused CAD view omits only the known below-first-floor context fragment for readability; the native CAD retains it.
- FF04 and FF06 room extracts pass exact native reopen and STEP validity, solid count, bounds, volume, area and summed-edge-length checks. Thirteen other room sets retain their verified R02 geometry bytes. R01 and all 21 older concept images remain unchanged.
- The full first-floor CAD and both room STEP files exceed Pages' single-file limit. They are available in the public `building-models-r03` GitHub release, and downloaded bytes match their accepted hashes. The website remains a separate release gate until its checks and production deployment complete.

The user confirmed on 10 September 2026: latest approved Blender design first, coordinated CAD second, concept images third. Original site photographs remain evidence of existing conditions, not editable as-built records. An unissued Blender draft does not supersede a selected release.

Six detail views in `public/building-vision/model-aligned-r01/` come directly from R01 whole-floor Blender geometry. They remain labelled R01, never regenerated or relabelled R03. GF10/GF09/GF08 arrangements are retained; the FF04 central-door image is historical and superseded by R03's left curved stair slider. These views precede expandable older concepts on cards 02, 03, 06, 10, 11 and 13. Their source and image hashes are in `provenance.json`. All 21 earlier PNGs remain unchanged. Use the R03 planning section for current floor previews, browser models and room CAD.

Before publication, run `node scripts/building-vision/strip-image-path-metadata.mjs` on reviewed native PNG copies. It removes Blender text metadata containing local file paths, preserves pixel/color-profile chunks byte-for-byte and records both source-image and sanitized hashes. The normal release check rejects text metadata and verifies pixel hashes. Never run this against source photographs or original render files.

The audit found round GF10 tables and chair layouts, straight-front stair partitions, and GF08 enclosure/door/step details conflicting with the selected model. GF01's eight-seat concept, FF02's pictured glass roof and the earlier first-floor office concepts are not current geometry. Keep those images visibly labelled as historical; do not fabricate completion. R03 retains the accepted approximate FF06 dresser/mirror and cupboard while replacing the former four-desk proposal with the selected twin cabins; retained storage is not newly measured.

An AI-edited first-floor partition test was not promoted: its frame shape improved, but the circulation strip appeared narrower than the native design. Use direct native renders when photo editing cannot preserve geometry. Future photo revisions must pass checks for furniture counts, wall/curve transitions, door locations, stair voids, frame path and access before replacing a reference.

## Selected P01 baseline history — superseded entrance details

The following records establish the preserved P01 baseline, not the current P03 door design or current release hashes. P03's final source/public hashes and verification records belong in the R03 manifest and curation provenance. The right FF04 and both FF06 outward entrances below are superseded by the user's sliding-door approval. Do not publish the P01/P02 historical native files as the current download.

- The earlier publication target was R03 on 11 September 2026; the final sliding-entrance release is dated 12 September. R01 remains immutable public history; R02 was not published.
- Services: numerical S01 discussion scenario of 10 September retained, **not recalculated for R03**. Room purpose/status/holds are reviewed separately. The old FF03 two-desk allowance and FF04/FF06 assumptions do not establish service provision for the selected six-/eight-desk programmes. Not surveyed installation or electrical design.
- Source Blender: `Armature - FF04 FF06 Selected Cabins - P01.blend` (SHA-256 `56b8e2fffc3dbc7239db3b0346d63bf82f05460d1f7001410aa6e2669a90c5e8`). Selected ground scene: `GF01 A P01 | 03 Ground floor - cumulative`; selected first scene: `FF04 FF06 P01 | 05 First floor - cumulative`. Earlier scenes remain historical, not alternative current selections.
- Source CAD: retained GF01 P01 cumulative ground floor and the new FF04/FF06 P01 first-floor model. `First Floor - Current Aligned Review P01.FCStd` (SHA-256 `ce11502242e56f102694a1da4e89c165f100ab14462dd8d77034e2c679a8e67b`) is the presentation-only current view of `First Floor - FF04 and FF06 Selected Cabins P01.FCStd`; identical-geometry, visibility and fresh-reopen gates passed. The public design hold still applies. Record source/candidate/public-copy hashes separately before any future release. The earlier interrupted GF01 handoff required display-metadata completion on task-owned copies, without changing geometry or originals.
- Release assets: `public/building-models/r03/`; hashes in `release.json`. Local assets are not proof of live publication.
- Room service data: `src/data/buildingRoomServices.json`.
- Native full-floor CAD is FreeCAD. Legacy R01 full-floor-named STEP files cover stair partitions only; never describe them as full-floor CAD.
- Room CAD extracts retain exact selected source B-reps and world coordinates; they do not retain the full document's parametric dependency history. Shared openings are repeated as context in adjoining rooms.
- R03 retains GF01 cabin A P01, FF02 workshop P01 (two square tables/four chairs, separate bench and provisional enclosure/cover), FF03 P01 two- and four-person cabins, and FF06 B02 balcony cleanup. It adds selected FF04 and FF06 twin cabins: eight full-size tables and eight chair proxies in each room, not certified simultaneous capacity.
- FF04 uses two four-table islands and the selected enlargement toward the stairs, including the right cabin's 1 ft 6 in gain. The left cabin slides; the right cabin opens outward to the right. A custom two-leaf curved slider opens at the left stair section; glazing closes the former central door. Separate 1.880 m and 1.930 m leaf radii and exact selected parked paths are retained. The all-sliding alternative is not the selection.
- FF06 uses revised A: an enlarged upper cabin, a lower four-table row with dedicated balcony access, two outward doors and shared west cupboard access. Its approximate dresser/mirror, cupboard and corrected B02 balcony remain. Only the old four-desk proposal and old FF04 partition are replaced in the new cumulative scene; originals remain preserved.
- GF01 retains constrained pull-back/shared-door routes. FF03 retains the nominal 2 ft shared cross-route, tight rear-chair gap and obstructing west outward-door swing. FF04 island access and FF06 upper chair pull-back/door-to-cupboard constraints remain unresolved. Sliders do not establish usable circulation, net clear openings, hardware feasibility or egress approval. An unconfirmed opening shift is not included.
- The FF04/FF06 native CAD static-solid audit found 36 positive door/frame component overlaps at the modeled hinge/jamb and curved-slider return interfaces. No tested stored-chair solid collisions or boolean errors were found; 52 open-reference candidate pairs could not be solid-tested. This is a static-pose check, not a continuous door sweep or an occupied-person test. Preserve and disclose these unresolved interfaces rather than moving selected geometry during publication.

The selected Blender source passed fresh reopen: 4,484 original objects, 41 original scenes and 80 original materials preserved; all 910 inherited visible physical objects retained without reactivating hidden geometry. The 507 new components and 265 cropped context objects match the saved geometry export. Independent room/full-floor render review confirmed furniture, doors and retained context. The extra FF04 presentation overview hides only a proven below-first-floor context fragment for that render; the saved source is unchanged.

Native CAD fresh reopen also passed: 1,331 baseline objects preserved, 2,454 source-geometry checks, and physical/open-reference STEP round-trip checks for FF04, FF06 and the complete first-floor source context. All ten native/STEP file hashes matched the audit. Independent visual review covered six annotated genuine FreeCAD plan/axonometric exports; table seams, selected layouts and private/unresolved-interface labels were readable. These are source-fidelity and export checks, not door-operation approval. Sanitized public-copy and production results remain separate future gates; no such release is claimed while the door-detail hold remains.

Authority was checked against the task **Reimagine Armature Lab building**, including FF04 enlargement/door direction `01a08f8e-bc8f-70d3-8e2d-474090c4470b`, FF06 dedicated-balcony revised A `01a08eb6-6e29-7b22-bdc3-0a60fdb762ef`, GF01 native continuation `01a08fde-86fa-7192-9c96-abbec2943feb`, FF03 selection `01a08ed1-144b-7030-ade2-b98d3f0564ba`, FF02 implementation `01a08c92-57a6-7cd3-892e-07aaf168baaf`, and FF06 B02 cleanup `01a08e51-e212-79a1-a5aa-4ba1135cf79a`. The later explicit FF04/FF06 native-render request superseded the earlier line-only implementation hold. The user initially approved a public GitHub native-CAD destination while holding publication until door details were revised. Their subsequent explicit sliding-door and publication approval supersedes that historical hold, subject to the P03 verification gates above. A stale R01 registry does not override this authority or fresh native evidence.

## Update procedure

1. Save and reopen the newly verified native Blender/CAD release, preserving earlier files. Hash the sources and record the approvals and remaining limitations. Never choose a file solely by modification time.
2. Export affected selected floor scenes to browser GLB and generate their previews from Blender. For unchanged floors, retain the published files and their original provenance, then verify them against the new cumulative source. Exclude hidden alternatives and duplicate scene objects. Do not overwrite an already-published version directory; use a new release ID and explicit per-asset references.
3. Regenerate affected room FCStd, STEP and SVG extracts from copied native CAD sources. Audit object membership, openings and save/reopen validity. Include all 15 retained room IDs unless scope is explicitly changed.
4. Update the room service schedule separately. Keep proposed versus installed counts, explicit arithmetic, HVAC exclusions and unresolved access/safety items visible. Review coordinated room changes even when a service count stays unchanged.
   Audit the 21 image references against Blender first and CAD second. Regenerate affected native views and their provenance; update each image's current/pending note. Preserve source photographs and old versions. Do not use generated imagery to establish dimensions or erase an unresolved constraint.
5. Create separate current-design-only public native copies, retaining exactly the selected floor geometry, materials, world positions and required dependencies. Exclude failed trials and old alternatives; preserve them in the private sources. Disclose any loss of historical parametric dependencies. Sanitize local paths, render-output paths and inactive console history, and independently reopen to verify retained geometry, membership and review views. Record display-only camera changes explicitly. Strip PNG text metadata on copies without changing pixel/color chunks. Record source/public hashes and curation proof; do not use metadata-only equality claims for curated files or publish unsanitized history in Git.
6. Run `npm run prepare:building-models` after review, then `npm run check:building-models`, `npm test`, `npm run build`, existing browser tests, and a visual check of both GLBs, room selectors, downloads and all themes at desktop/mobile widths. Confirm no GLB request before explicit activation.
7. Publish through the repository's reviewed-main production workflow with a squash merge. Never bypass failing security/release gates. Check the live page and compare actual downloaded bytes against `release.json` after deployment. External GitHub files require downloaded-byte proof before the release checker passes; availability alone is insufficient.

This is an explicit, reproducible publication workflow, not a background watcher. Saving a local file alone does not update the public website. Keep the last public revision visible until the next coordinated release passes its checks.

## Performance and evidence boundary

The new-code contract is one active floor model at a time, no GLB transfer before opening the viewer, and readable previews/downloads without WebGL. Validate on the available macOS Chromium desktop and a 390px emulated viewport; this does not establish phone GPU performance. Load failures must leave a useful fallback. Do not claim photorealistic parity: procedural Blender materials and light rigs differ from GLB/PBR browser shading.
