# Website work progress — 26 September 2026

This is an evidence log for a later independent review. It records completed changes, checks, and open release gates; it does not treat a local build or merged PR as proof of a live deployment.

## Scope and source

- The original checkout at `/Users/dev/Downloads/Armature Lab` contains unrelated local changes and was left untouched. Work uses isolated branches based on GitHub `main`.
- PR #73 (team memberships and booking) and creation of a separate Supabase preview project remain on hold at the owner's request. No live Supabase schema or booking integration was changed in this sequence.

## Who We Are

- Selected design 03 was already prepared in [PR #71](https://github.com/sandeep-devarapalli/armature-ai-labs/pull/71). It adds `/about/`, a separate `/team/` placeholder without invented profiles, original illustrations, positioning notes, and route metadata.
- Fresh validation on 26 September: `npm test` (71 passed), `npm run build:demo` (passed, including release-artifact checks), and `npx playwright test tests/frontend/e2e/routes.spec.ts --project=chromium --project=mobile` (94 passed, 2 intentional skips).
- PR #71 was squash-merged as `64273785b06af21a555669a95ca38679c8a658dd`. Main-branch frontend and database CI passed; `deploy-production` is waiting for the protected production environment's owner approval. This is not live-deployment evidence.

## SEO and dependency work

- The reconciled SEO work in [PR #74](https://github.com/sandeep-devarapalli/armature-ai-labs/pull/74) was squash-merged as `1658414a0b68deb01e0fbc9ad036657d7f19d21f`. Its PR frontend/database CI and main-branch frontend/database CI passed. The main run `36215479310` is waiting at `deploy-production`; no live deployment was verified.
- Earlier PR #63 was built before the currently approved T2 scenes, square logos, favicon and MHS cover. A direct merge would overwrite those assets. Its prerender, sitemap, route metadata, redirects and HTTP checks were ported to `codex/seo-current` from merge commit `6427378`, with the current `/about/` indexed and `/team/` prerendered but noindex. The MHS article keeps the selected 1672 × 941 Common interface cover in Open Graph, Twitter and BlogPosting data.
- The older PR's reviewed MHS engineering revision and click-to-play video were carried forward without replacing the approved cover. Homepage, membership, services, ecosystem and electrical-plan wording now distinguishes planned facilities and resources from operating ones. The newer T2 video placements remain intact.
- `/about/` and `/team/` use the shared route metadata after hydration; the older page-local title effect was removed so navigation cannot race with the SEO head manager. The final desktop/mobile SEO browser rerun passed all four checks, including About and Team transitions.
- SEO validation: `npm run build:demo` and `npm run build` both passed (153 indexable HTML pages, 155 prerendered paths including noindex pages, sitemap, robots, asset checks); `npm test` passed 107 tests. The local Cloudflare runtime passed 20 HTTP probes on both demo and non-demo builds. The combined desktop/mobile SEO, route and theme suite had 108 passes, 2 intentional skips and four outdated test assertions; all four passed after updating selectors for the source-linked list and demo-mode membership copy. The non-demo release-gate suite had 15 passes, one intentional skip and two stale copy assertions; both passed in the focused rerun. `npm run audit:all` found 0 vulnerabilities on the locked dependencies.
- [PR #72](https://github.com/sandeep-devarapalli/armature-ai-labs/pull/72) changed only the pinned `actions/upload-artifact` action and was squash-merged as `3373f76a9f90fc57fdebc2dacd7b74e7c8076ea7` after fresh frontend/database CI. Main-branch checks passed; deployment is waiting for owner approval.
- [PR #75](https://github.com/sandeep-devarapalli/armature-ai-labs/pull/75) replaced stale MapLibre PR #37 with `maplibre-gl` 6.11.2. It was squash-merged as `2ed86b14d9601d5e98db493de7eda98775deeabe` after `npm test` (71 passed), `npm run build:demo`, desktop/mobile map interaction tests (2 passed), and fresh frontend/database CI. Its main checks passed; deployment is waiting for owner approval. PR #37 was closed.
- [PR #76](https://github.com/sandeep-devarapalli/armature-ai-labs/pull/76) replaced stale Lucide PR #36 with `lucide-react` 1.47.0. It was squash-merged as `fb04e8e2f12ea374ff093c336b2bf6ef97989b9e` after fresh frontend/database CI. The combined MapLibre/Lucide branch passed `npm test` (107 passed), `npm run build:demo`, and desktop/mobile map and link-colour checks (8 passed). The earlier full desktop/mobile route suite passed 94 checks with 2 intentional skips. A local rendered homepage inspection showed the header, calendar, external-link and close icons. PR #36 was closed.
- The first PR #76 CI run caught an intermittent light-theme citation hover assertion: CSS smooth scrolling could continue after Playwright moved the pointer to the link. It reproduced in 2 of 8 parallel local repeats. The test now uses reduced motion to finish scrolling before checking hover colour; no site styling changed. All 30 repeated link-colour checks across three themes and two viewports passed, then the amended PR passed full CI.
- PR #63 and visually superseded PR #57 were closed after their useful SEO, MHS and pre-launch factual corrections were carried into #74. Do not merge #35 (`@zxing/library` requires Node 24), #34 (Node 26 types), or #14 (TypeScript 7 migration) as routine Node 22 updates. These remain open for a separate compatibility review.

## Release gate

- Main frontend/database CI passed for #71, #72, #74, #75 and #76. The #76 main run is `36216517267`; its `deploy-production` job is waiting for owner approval.
- Protected `deploy-production` jobs are waiting for owner approval. A merged PR or passing CI is not evidence that `armatureailabs.com` has changed. Approve only the latest desired main commit after checks pass, then verify the live page, article metadata, sitemap and assets by HTTP response.

## Verification for Claude

Use a clean checkout of current `main`; the original `/Users/dev/Downloads/Armature Lab` checkout has unrelated local changes. Review PRs #71, #72, #74, #75 and #76 and their squash commits above. Run `npm ci`, `npm test`, `npm run build:demo`, `npm run build`, and `npm run check:pages-runtime`; for browser checks, run `npx playwright test tests/frontend/e2e/seo.spec.ts tests/frontend/e2e/routes.spec.ts --project=chromium --project=mobile`. Compare generated `dist` HTML, sitemap, robots and redirects with the checked URLs. Verify current images and motion assets remain byte-identical where the SEO work does not intentionally change them. Read GitHub main CI, production deployment and live HTTP responses as separate evidence gates.

## Follow-up: indexing and draft team preparation

- The final public release deployed successfully as `a17089245c49a302578075e2928f19f64c134338` in GitHub run `36219213624`. The live `node scripts/check-seo-http.mjs https://armatureailabs.com` check passed all 20 probes on 26 September. This supersedes the earlier waiting-deployment statements above.
- In the verified `armatureailabs.com` Google Search Console domain property, signed in as `hello@armatureailabs.com`, `https://armatureailabs.com/sitemap.xml` was submitted on 26 September. Its detail page reported **Sitemap processed successfully**, last read 26 September, and **153 discovered pages**. This is discovery evidence, not proof that pages are indexed. The separate `sandeep.devarapalli@gmail.com` Search Console account showed no properties; the owner provided the correct Chrome profile.
- Bing Webmaster Tools accepted the same sitemap in its existing `https://armatureailabs.com/` property on 26 September. The readback showed one known sitemap, zero errors and warnings, and **Submitted - Processing**; no crawl or discovered URL count had appeared yet. Verify Bing processing later rather than calling submission an indexing result.
- Draft [PR #73](https://github.com/sandeep-devarapalli/armature-ai-labs/pull/73) was reconciled with current `main` in `/private/tmp/armature-team-membership`; merge commit `6bb4558` preserves the public `/team` page and moves private team routes into the shared route table. The original dirty checkout was not edited. This resumes local preparation only; the paid preview project, live schema, launch flags and outbound mail remain held.
- Production-disabled builds now skip the new `list_my_team_access` RPC, so an existing signed-in session does not require the unapplied team migration. A release-gate browser test covers `/workspace/team`, invitation acceptance and `/admin/teams` alongside the other operational routes.
- Node 22.22.2 local checks: `npm ci` reported zero vulnerabilities; `npm test -- --run` passed 112 tests; `npm run build:demo` and `npm run build` passed, including 153 initial-HTML public pages and the release-asset checks. `npx playwright test` on the demo build passed 141 desktop/mobile checks with seven intentional skips. The focused production build release-gate and SEO browser run passed 17 with one intentional skip.
- The isolated local Supabase stack applied `202609250001_team_membership.sql`. `supabase test db supabase/tests/database` passed **212/212** assertions, including 44 team assertions; `supabase/tests/concurrent_booking.sh` and `supabase/tests/concurrent_team_seat.sh` both passed. The new seat race test runs in draft PR CI. No live Supabase project was changed.
- Mail tests use mocked Gmail responses: a pending Send mail as alias blocks sending, and an accepted alias permits an encoded reminder request. No real mail was sent. Outbound alias, reply delivery, Gmail/Calendar delegation and preview/production authenticated flows are still unverified. Gmail has no send idempotency key; a worker crash after accepted delivery can cause a duplicate reminder on retry. Keep the Gmail provider and membership launch disabled until these checks are resolved.
- In the owner's signed-in Work Chrome profile on 26 September, Gmail Settings → Accounts → Send mail as listed only `Sandeep Devarapalli <hello@armatureailabs.com>`, with an `Add another email address` option. It did **not** list `bookings@armatureailabs.com`. The Google Admin screenshot established `bookings@` as an alternate inbound address, but that alone does not establish outbound Send mail as readiness. No alias setting was changed or message sent. Before launch, configure and verify `bookings@` for outbound sending, then test a real reply and Google delegation without broadening privileges by assumption.
- Later the same day, the owner added Send mail as entries. A fresh Gmail Accounts readback showed `bookings@armatureailabs.com`, `privacy@armatureailabs.com`, and `sandeep@armatureailabs.com` alongside default `hello@armatureailabs.com`, with no pending-verification label on the booking entry. “Reply from the same address to which the message was sent” was selected. This clears the missing Gmail UI configuration finding above; it does not yet prove the app's delegated Gmail API can send, that a recipient receives mail from `bookings@`, or that a reply returns to the shared inbox. No message was sent during this check.
- An authenticated browser journey against isolated local Supabase found and fixed a UI defect in commit `b92c392`: creating an invitation succeeded but calling global `refreshMember()` remounted `/workspace/team` and erased its one-time link. `TeamWorkspacePage.invite()` now refreshes the roster locally and displays the link after that refresh. The local-only Playwright test `tests/frontend/e2e/team-membership-local.spec.ts` passed on Chromium desktop and mobile (one test each, about 6.3 seconds per project). With synthetic staff, admin and builder accounts, it verifies staff application approval and activation, invitation creation and acceptance, team-seat booking, persisted team attribution, admin usage visibility, member removal and cancellation of the affected booking. The test refuses a non-local API/DB and requires `ARMATURE_LOCAL_TEAM_E2E=1`; it does not exercise live Supabase or email. Node 22.22.2 `npm run build` passed after the fix, `npm test -- --run` passed 112/112, and `git diff --check` passed. To rerun, start local Supabase, build or serve the app with its local API/anon key and `VITE_MEMBER_PLATFORM_ENABLED=true`, set `ARMATURE_LOCAL_SUPABASE_URL`, `ARMATURE_LOCAL_SUPABASE_ANON_KEY`, `ARMATURE_LOCAL_SUPABASE_SERVICE_KEY`, and `ARMATURE_LOCAL_SUPABASE_DB_URL` from `supabase status -o env`, then run `ARMATURE_LOCAL_TEAM_E2E=1 npx playwright test tests/frontend/e2e/team-membership-local.spec.ts --project=chromium --project=mobile`.
- For independent review, inspect PR #73's latest diff and CI, rerun the commands above on its head, and confirm the Search Console/Bing sitemap statuses in their respective dashboards. The team browser test covers local demo presentation; database tests cover the permission and RPC behavior. Neither substitutes for preview end-to-end testing before launch.

## Booking email and reminder follow-up

- Owner authorized one booking-alias delivery/reply test. On 26 September at approximately 12:53–12:56 IST, Work Gmail sent `Armature bookings alias test — AAIL-20260926` from `bookings@armatureailabs.com` to the owner's personal Gmail. The recipient UI showed the booking sender, and the shared `hello@armatureailabs.com` inbox showed both the owner's acknowledgement and the explicit reply-path test. This verifies the Gmail UI alias and inbound reply routing, not delegated API sending. No booking was created.
- Read-only Google Admin inspection at `/ac/owl/domainwidedelegation` showed an empty API clients table. `supabase secrets list --project-ref uxfhdfagrmaeyuaipaar` showed no `GOOGLE_SERVICE_ACCOUNT_JSON`, `GOOGLE_WORKSPACE_SUBJECT`, or `REMINDER_PROVIDER`. No secrets, delegation grants or live configuration were changed. App-originated Gmail and Calendar verification is blocked until an owner-approved service account/delegation and secure runtime configuration exist. Resource-calendar access and app-originated booking/reschedule/cancel delivery still require testing.
- Refreshed Bing Webmaster Tools now reports **Success**, last crawl 26 September, **153 URLs discovered**, zero sitemap errors and zero warnings. This supersedes the earlier Processing state; discovery remains separate from indexing.
- Added `202609260001_gmail_delivery_guard.sql`: a current claim token is required to mark a reminder `review_required` immediately before Gmail POST. The claim query excludes fenced rows even after failure or lease recovery. Confirmed completion marks the fence `sent`. This prevents automatic duplicate send attempts; it does not guarantee exactly-once delivery. A crash before POST can leave an unsent reminder requiring operator review. The operations runbook records deployment order and reconciliation; webhook receiver idempotency remains its own requirement.
- Validation on the changed tree, based on head `6dcb0ce`: Node 22 `npm test -- --run` passed **115/115**; `supabase test db supabase/tests/database` passed **224/224**, including 12 new guard assertions; `supabase/tests/concurrent_booking.sh` and `supabase/tests/concurrent_team_seat.sh` passed; `npm run build` and artifact checks passed; `git diff --check` passed. Local Supabase was stopped after validation. Earlier desktop/mobile team and release-gate results above remain applicable; this change does not alter frontend UI.
- PR #73 remains draft. Production membership, live migrations, automatic Gmail delivery and paid preview infrastructure remain disabled/deferred. Next launch gates: securely establish Google service account and explicit delegation, verify actual API send/reply and resource calendars, complete preview integration checks, then obtain launch authorization. No password should be placed in chat or source control.

## Google connection preparation

- Fresh GitHub readback confirmed head `b2d7aade0570e2fd05f26d1d9e843ec0c6c07e19` passed frontend and database in run `36226837806`; production deployment was skipped.
- Opened Google Cloud Service Accounts in the owner's Work Chrome profile. Google required password re-verification for `hello@armatureailabs.com` before showing Cloud resources. Awaiting owner sign-in directly in Google; no password requested in chat, no service account or delegation created, and no production configuration changed.

- Google Cloud project creation was explicitly approved and completed: `Armature Booking Integration`, ID `armature-booking-integration`, project number `863263629225`, using `hello@armatureailabs.com`. No billing activation or free trial was performed. Work-profile browser extension now permits tab-scoped control. Prepared, but did not submit, the `Armature Booking Service` account form (`armature-bookings`); no service-account keys, roles, or Workspace delegation have been created. Awaiting explicit service-identity creation approval.
- Owner-approved service account created and verified enabled: `armature-bookings@armature-booking-integration.iam.gserviceaccount.com`, OAuth client ID `107604993910828147467`. Creation skipped optional roles/access steps; list reports No keys. Workspace delegation form is prepared (not authorised) for `gmail.send`, `gmail.settings.basic`, and `calendar`, matching current code. Explicit action-time delegation approval is pending; domain-wide delegation itself is not limited to hello even though app code enforces that subject. No live runtime settings changed.
- After explicit delegation approval and owner password re-verification, Google Admin confirmed client `107604993910828147467` was added with three scopes: `https://www.googleapis.com/auth/gmail.send`, `https://www.googleapis.com/auth/gmail.settings.basic`, `https://www.googleapis.com/auth/calendar`. Saved row identifies the expected service-account email. Credential creation and runtime configuration remain outstanding; no API send or Calendar lifecycle test is yet claimed.
- Key creation failed because `iam.managed.disableServiceAccountKeyCreation` is enforced. Read-only policy inspection confirms the booking project inherits the parent policy; both Manage policy and Simulate policy are disabled for the current account. No policy was relaxed and no key was created. An appropriately authorized organization-policy administrator must prepare a narrowly scoped exception before owner credential creation can proceed. Do not disable the restriction organization-wide by default.
- With explicit owner approval, granted `roles/orgpolicy.policyAdmin` to `hello@armatureailabs.com` on organization `armatureailabs.com` (`720360649406`). IAM readback shows Organisation Policy Administrator and a successful policy-update notification. No service-account roles were granted. Preparing only a booking-project key-creation exception; no organization-wide restriction has been disabled.
- Owner explicitly approved the project-only exception. Saved `iam.managed.disableServiceAccountKeyCreation` on `armature-booking-integration` with Override parent's policy / Not enforced. Google readback confirms effective status Not enforced and successful update notification. The organization policy itself was not edited. No key was generated by the agent; owner credential-creation handoff remains required. Restore project inheritance after the required key is created and securely configured, subject to owner authorization; this does not revoke an existing key.
- Located owner-created JSON in Downloads and validated expected service-account/project/client identity without displaying its private key. Cloud Keys page reports an active generated key. Local signed-JWT authentication as hello succeeded (HTTP 200). Initial read-only Gmail SendAs and Calendar list probes returned SERVICE_DISABLED (403). Subsequent Console readback shows Gmail API Enabled; Calendar API still offers Enable. No email/event was sent or created, and no credential was uploaded to Supabase. Calendar activation awaits approval of its displayed API terms.
- Owner approved Calendar API activation. Enabled `calendar-json.googleapis.com`; Console status is Enabled. Repeated local read-only probes succeeded: delegated token HTTP 200, Gmail SendAs lookup HTTP 200 with `verificationStatus=accepted`, Calendar list HTTP 200. No token/private key was printed or persisted in the repository. These prove authentication and API access, not outbound API delivery or calendar create/update/cancel behavior. Supabase credentials/jobs and production membership remain unchanged.

## Controlled Google API test — 26 September, 14:34 IST

- Owner instructed proceeding with the remaining configuration/tests. Executed the application helpers from commit `ebfe72a` locally, with service-account JSON supplied only through an in-memory `Deno.env` adapter. No credential was written to the repository, printed, or uploaded to Supabase.
- Command: Node 22.22.2 `node --experimental-transform-types /private/tmp/armature-google-controlled-test.mjs`. The initial strip-only attempt failed at TypeScript parameter-property parsing before API calls; transformation mode resolved that harness issue without application changes.
- Test ID `f8d11e44-e800-4921-a418-745272fed722`: `googleCalendarRequest` created a temporary transparent event on hello's primary calendar (HTTP 200), updated it (200), read back the matching updated summary (200), deleted it (204), and confirmed its returned status `cancelled`. No attendees, default reminders, or notification updates were enabled. This does not verify per-resource calendar sharing or booking outbox processing.
- `sendBookingMail` checked the accepted alias, called its before-send callback, and successfully submitted one clearly labelled API integration test from bookings to hello. Work-profile Gmail search for the exact test ID showed one received Inbox message at 14:34 IST. This verifies API acceptance and internal inbox receipt. External recipient/reply routing was tested earlier through Gmail UI; this run does not repeat that external journey or exercise the database reminder fence.
- Harness and sanitized HTTP evidence remain at `/private/tmp/armature-google-controlled-test.mjs` and `/private/tmp/armature-google-controlled-test-result.json`; the latter records API results only and predates browser receipt confirmation. Do not rerun the harness merely to inspect evidence: it sends a new email.
- Production secret upload remains deferred: Google credentials are shared by calendar/reminder functions and may activate existing workers independently of the public membership flag. Before uploading to `armature-lab`, inventory deployed functions/schedules and pending work, establish an explicit disabled-worker gate, and approve deployment/migration order for the reminder guard. Do not infer safety from the frontend feature flag. Paid preview infrastructure and production membership remain held.
- Remaining launch checks: per-resource private calendar IDs/access, full isolated booking create/reschedule/cancel integration, durable reminder guard deployment and reconciliation, safe managed-runtime configuration, and explicit membership/automatic-delivery launch authorization. Restore the project key-creation restriction after secure credential custody is settled.

## Production worker inventory and safe credential staging — 26 September

- Read-only `supabase functions list --project-ref uxfhdfagrmaeyuaipaar` found active version 5 calendar-sync and retry-reminders, alongside kiosk and booking-ics. Downloaded deployed worker source into `/private/tmp/armature-live-worker-audit` using `functions download --use-api`; production calendar has no disabled-worker gate, while production reminders are webhook-only. Draft Gmail code is not deployed.
- Secret-name inventory still has no Google service-account JSON, Workspace subject, or reminder provider. No secret values were retrieved. Missing frontend membership access is not a worker execution gate.
- Linked only the isolated audit folder to obtain IPv4 connection metadata after the CLI's initial IPv6 error. Used `supabase db query --linked --workdir /private/tmp/armature-live-worker-audit` with an explicit read-only transaction and one aggregate result: zero integration-outbox rows, zero reminder rows, zero active calendar links, and neither pg_cron nor pg_net installed. No row details were fetched. GitHub workflow inventory showed CI and Dependabot; checked repository workflow source has no booking schedule. Other external scheduler ownership cannot be established from those inventories alone.
- Added default-off `BOOKING_WORKERS_ENABLED` checks immediately after job-secret authentication and before database work in both draft workers. Only exact `true` enables them. Disabled retry-reminders also skips attendance maintenance. Node 22 unit suite passed 123/123 including eight handler tests across both workers, verifying no DB/network use while disabled; production build and artifact checks passed; `git diff --check` passed.
- Prepared the identical three-line checks against downloaded production v5 source in the isolated audit folder, preserving each original as `index.original.ts`. This proposed safety-only deployment must use that deployed baseline, not the draft workers with unapplied migration requirements. No production function, credential, schedule, schema or membership setting was changed in this turn.
- Concrete next gate: approve the two-worker safety-only deployment; verify authenticated POSTs return disabled with no queue work; then store the Google JSON and hello subject in managed Supabase secrets while BOOKING_WORKERS_ENABLED remains false. This would stage credentials only. Keep membership release, migrations, Gmail provider activation and worker enablement separately held.

## Approved safety deployment and credential staging — 26 September

- Owner explicitly approved the safety-only deployment and Google credential storage while keeping membership and automatic delivery held. Set `BOOKING_WORKERS_ENABLED=false`, then deployed only calendar-sync and retry-reminders from `/private/tmp/armature-live-worker-audit` with `--use-api --no-verify-jwt`, preserving their existing gateway JWT setting and application job-secret authentication. Each entrypoint differs from downloaded production baseline by only the three-line disabled check. No draft membership code, migrations, or Gmail reminder implementation was deployed.
- Downloaded both deployed functions to `/private/tmp/armature-worker-deployed-verification`; each entrypoint matched its prepared patched source byte-for-byte. The prior local 123-test suite includes disabled-handler behavior; no new application edits were made for this deployment.
- Stored `GOOGLE_SERVICE_ACCOUNT_JSON`, `GOOGLE_WORKSPACE_SUBJECT=hello@armatureailabs.com`, `REMINDER_FROM=bookings@armatureailabs.com`, and `BOOKING_WORKERS_ENABLED=false` in project `uxfhdfagrmaeyuaipaar`. Used a mode-0600 temporary env file outside the repository, removed in a finally block. Compared returned secret digests against local SHA-256 values; all four matched. No private key, access token, or secret value was logged. Existing ARMATURE_JOB_SECRET digest and REMINDER_PROVIDER state were unchanged.
- After secret updates, function inventory reports calendar-sync/retry-reminders version 8 ACTIVE and kiosk/booking-ics version 7 ACTIVE. Only the two named worker functions were explicitly deployed; project-wide secret changes affect function runtime configuration. Unauthenticated POSTs to both workers returned HTTP 401. Read-only aggregate DB verification still reports zero outbox rows, zero reminder rows and zero active calendar links.
- Verification limit: existing job credential was not found in the checked project env files, and Vault reported zero matching job-secret entries. Therefore no authenticated live disabled-response probe was performed; do not treat the HTTP 401 checks as that proof. Did not rotate the job secret or broaden credential access to obtain it. Disabled state is supported by deployed-source readback, exact false secret digest, and local handler tests. Authenticated live verification remains required before worker activation.
- Production membership, scheduled delivery, worker enablement, migrations and the draft Gmail-provider deployment remain held. Google credentials are now staged; no API mail or calendar event was sent by this release. The earlier controlled local API tests remain separate evidence. Further launch work requires resource calendars, the current job credential for the authenticated probe, and the reviewed migration/function release sequence. Key-creation-policy restoration remains a separate outstanding security housekeeping action.

## Membership decisions and payment hold

- Recorded successive owner clarifications in the membership plan and AGENTS.md: free basic membership, mandatory LinkedIn, guardian email consent for ages 16–17, ID deletion 30 days after upload, dated day/week/calendar-month access, normal versus paid overnight hours, cabin visitor limits, event rules, optional weekly/monthly renewal and full refunds within agreed windows. Day passes never renew.
- Razorpay test-mode setup is now explicitly deferred while the owner changes the LLP name. No account creation, payment configuration or production activation was performed. A follow-up reminder is being scheduled; it asks about readiness rather than assuming merchant approval or authorizing setup.

## Local membership review implementation

- Added an isolated dev-and-demo-only `/membership-preview` for synthetic registration, staff/guardian review, document deletion simulation, dated passes, mock payment/refund and resource rule inspection. Uses transient component state, accepts no real ID files and makes no backend/payment calls. Production routes/credentials/workers remain unchanged.
- Local rules include minimum age16, own LinkedIn, reviewed guardian email for minors, calendar-month and seven-day validity, multi-date day passes without renewal, adults-only23:00–08:00 overnight, IST refund clocks and conservative handling of undefined refund policies. Resource checks cover guest rounding/duration, event capacity/buffers and provisional staff exceptions.
- Node22 unit tests131/131; production build/artifacts passed; dedicated desktop/mobile preview12/12; production release gates13 passed/1 intentional skip. Read `docs/membership-preview-handoff.md` for commands, screenshot paths, limitations and next server-side steps. This is reviewable UI/rules work, not completed live onboarding or automated ID deletion. Razorpay remains explicitly deferred during LLP rename.
